[package]
name = "bench"
version = "0.1.0"
edition = "2021"

[dependencies]
simulation = { path = "../simulation" }
bevy_ecs = "0.19"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "tick_bench"
harness = false

[[bench]]
name = "phase_bench"
harness = false
