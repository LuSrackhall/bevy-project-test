# 工作日志 2026-09-13

> 多 agent 共用，只追加不重写；新条目追加在文件末尾。

## 03:03 · workbuddy · 建立 AGENTS.md 唯一约定源 + 共享工作日志机制

- **改动**：
  - 新增 `AGENTS.md` — 全仓 agent 唯一约定源，共 4 节：第 0 节共享工作日志（读/写时机、条目格式、记录边界）、第 1 节 git 卫生（多会话并行的提交纪律）、第 2 节迁移自 `CLAUDE.md` 的架构宪法速查与 AI 自检清单、第 3 节技能与文档单真源、第 4 节文档体系。
  - 改写 `CLAUDE.md` — 退化为引用壳，正文仅剩 `@AGENTS.md` 导入与"规则别写这里"的说明，消除双真源。
  - 新增 `.workbuddy/memory/MEMORY.md`（项目长期约定）、`.workbuddy/memory/2026-09-13.md`（本日志）。
  - 本次 git 提交用路径限定（`git commit -- <paths>`）只含上述本会话产物，未触碰同一工作区内其它会话正在进行的 `crates/bevy_adapter/src/remote.rs`、`crates/simulation/src/lib.rs` 改动。
- **决策**：
  - 采用「单一真源 + 引用」，与本仓既有模式一致（`.claude/skills` 本就是指向 `.agents/skills` 的软链），而不是在 `CLAUDE.md` 与 `AGENTS.md` 各存一份准则。
  - `CLAUDE.md` 用 Claude Code 原生 `@` 导入语法而非符号链接：软链检出到 Windows 时可能退化成普通文件，`@` 导入跨平台安全；文件内已加"工具不解析 `@` 时请直读 `AGENTS.md`"的兜底。
  - 日志目录沿用 `.workbuddy/memory/`（工具默认路径），但在 `AGENTS.md` 中显式声明该目录为**全仓共享**、任何 agent 都应读写，避免非 WorkBuddy 的 agent 误判与己无关。
  - 迁移前用脚本逐条比对原 `CLAUDE.md` 的实质条目，缺失数 0，确认无条款丢失。
- **遗留**：
  - Claude Code 实际执行时能否正确解析 `@AGENTS.md` 导入尚未验证，下次在 Claude Code 会话中留意。
  - 多个会话并行写同一个当日日志文件时，追加冲突风险待观察；届时可考虑按 agent 拆分成 `YYYY-MM-DD-<agent>.md`。
  - `.workbuddy/` 目前只含 `memory/`，若未来 WorkBuddy 在该目录写入会话缓存类文件，需要加 `.gitignore` 白名单只跟踪 `memory/`。
  - 本仓其它协作面（`.github/workflows/`、`openspec/`、`.agents/skills/` 的 myspec 流程）尚未纳入 `AGENTS.md`，如需约束可后续补充。

## 03:37 · workbuddy · 调研 agent 记忆方案：文件派与向量派的适用边界

- **调研**（外部资料，本仓无代码改动）：
  - 社区并非铁板一块：Claude Code、Cursor、Windsurf 的官方记忆都是 markdown/本地文件；OpenClaw 走 SQLite + FTS5 + sqlite-vec 折中；Mem0、Zep/Graphiti 一类走向量 + 时序图。
  - 文件派的三条硬理由：① 零 false negative —— 读进 context 就一定生效，而向量检索受相似度阈值影响会**静默漏掉**记忆；② 可审计可修正 —— 改一行再 `git diff`，而向量库要经 API 找到对应 embedding 删除重插；③ 语义工作在写入时做一次，而非每次查询都付。
  - 文件派公开的失败模式：主流实现（Claude Code、Cline、Cursor、Windsurf）**均不处理并发写**；无语义排序；索引摘要过时会漏文件；规模天花板约在"单 agent/团队 5000 条记忆以内"。
  - 可偷的模式：`MEMORY.md` 退化为**索引**（一行一个文件），细节拆到主题文件按需加载，避免常驻文件无限膨胀。
- **对照本仓现状**：
  - 三层结构恰好对应社区总结的记忆分类：`AGENTS.md` + `.agents/skills/` = procedural、`MEMORY.md` = semantic、当日日志 = episodic。社区首要忠告是"三类不要混在一个 store"，此处天然分离。
  - 上文 03:03 条目遗留的"多会话同写当日日志"冲突风险，与社区公认的文件派失败模式**同源**，非本仓特有问题；若真出现冲突，按 agent 拆文件是已知解法。
  - 本仓相对社区同类方案的稀缺点：**记忆纳入 git** —— 可 diff / review / revert / 随代码进 PR。Claude Code 等的记忆默认不受版本控制，这一点是少数派做法。
- **遗留**：以下三项为候选改进，**尚未写入 `AGENTS.md`**，待用户确认后再动手 —— ① `MEMORY.md` 改为索引结构；② 明确 episodic / semantic / procedural 三类记忆各自的写入策略；③ 补规模判据与并发写的缓解手段。

## 04:16 · dsh · BRP 驱动的 UI 端到端验收 + 归档 3 个停滞 change

- **改动**：
  - `crates/bevy_adapter/src/remote.rs` — 新增 BRP 自定义方法 `city_conquest/screenshot`（官方 `Screenshot::primary_window()` + `save_to_disk`），agent 请求后截图落盘、可直接读取 PNG 做像素级断言。
  - `scripts/brp_ui_smoke.py`（新增）— BRP UI 端到端冒烟：探针 → 枚举 UI 文本 → 注入 `bevy_ui_widgets::Activate` 点击 → 断言 tick 推进 → 请求截图。实跑（真实运行中的客户端）：菜单文案 9 条可见、点击地图大小按钮后 tick 0 → 6、截图 1280x720 / 863 种颜色。
  - `openspec/changes/archive/2026-09-12-{discovery-domain-model,lan-room-list,multiplayer-ui}` — 3 个停滞 change 归档（核实均为"被后续实现取代"），并修正 multiplayer-ui delta spec 中 3 处缺 SHALL/MUST 的需求表述。
- **决策**：
  - 归档用 `--skip-specs`：multiplayer-ui 的 delta spec 描述的是已被删除的 `NetworkPlayerId`/`NetworkPlayerCount` 组件（能力已由 v2 大厅 `ModalPlayerCount` 承担），同步进主 specs 会把过时设计固化成契约。
  - 冒烟测试点"地图大小"而非"局域网模式"：前者不启动 LAN 发现监听器，避免 macOS 防火墙再次弹出入站授权询问。
  - 验证在独立 worktree + 独立 target 内完成（当时 main 工作树含他人未完成重构，编译不通过）；只提交自己的两个路径，未触碰他人 12 个已改文件。
- **遗留**：
  - `brp-ui-work` 分支仍存在（本次验证产物，未删除，删分支需用户批准）。
  - 截图目前只能用像素统计判断"是否空白"；要断言"UI 长什么样"需图像能力模型直接读图。
  - 直接运行二进制需 `BEVY_ASSET_ROOT=<repo>`，否则 Bevy 以可执行文件目录为资源根、找不到 `assets/`，UI 文本会全部缺失。
