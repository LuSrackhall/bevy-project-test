# 兵种行为重设计 — 盾牌系统 & 弓兵修正

> **日期**: 2026-06-13
> **状态**: 待审批
> **范围**: 盾牌系统全新设计、弓兵行为修正、设计文档同步更新

---

## 1. 背景

经对比设计文档 (`rts-game-design.md`) 与当前实现代码，发现以下差异需要统一：

| # | 差异 | 决策 |
|---|------|------|
| 1 | 盾牌模式需要完全重设计 | 全新盾牌物品系统 |
| 2 | 弓兵 multi-shot 未实现 | 补齐实现 |
| 3 | 弓兵站桩不动（设计要求追击到射程边缘） | 修改行为 |
| 4 | 箭矢追踪 vs 固定方向冲突 | 确认固定方向，更新设计文档 |
| 5 | 射程固定 vs 等级缩放冲突 | 确认等级缩放，更新设计文档 |
| 6 | 弓兵近距离伤害惩罚 | 移除 |
| 7 | 步兵对弓箭 10% 被动减伤 | 移除（盾牌系统替代） |

---

## 2. 盾牌系统（全新设计）

### 2.1 核心概念

盾牌是一个**独立物品**，拥有自己的 HP（白色血条，区别于士兵的绿色血条）。盾牌可以被捡起、掉落、破碎，并在城池光环范围内回复 HP。

### 2.2 数据结构

**simulation 层新增/修改组件：**

```rust
/// 盾牌物品数据（挂载在士兵实体上，或作为 DroppedShield 的子数据）
#[derive(Component)]
pub struct ShieldItem {
    pub hp: u32,
    pub max_hp: u32,
}

/// 盾牌状态（仅持有盾牌的士兵有此组件）
#[derive(Component)]
pub enum ShieldState {
    Normal,     // 普通状态
    Blocking,   // 手动格挡中（仅步兵可切换）
}

/// 地面上的掉落盾牌实体
#[derive(Component)]
pub struct DroppedShield {
    pub shield: ShieldItem,
    pub position: FixedVec2,
    pub drop_tick: u32,                   // 掉落时的 tick（用于 30s 计时）
    pub owner_faction: Option<Faction>,   // 掉落者阵营（渲染用）
}
```

**士兵组件变更：**
- `ShieldComponent` → 改为 `Option<ShieldItem>`（士兵可能没有盾牌）
- 保留 `ShieldState` 组件（仅持有盾牌的士兵有意义）

### 2.3 基础数值

| 参数 | 值 | 来源 |
|------|-----|------|
| 盾牌初始 HP | 1500 | 需新增配置项 |
| 掉落存活时间 | 30 秒 (600 ticks) | 需新增配置项 |
| 消失动画时长 | 3 秒 (60 ticks) | 需新增配置项 |
| 被动格挡概率 | 40% | 需新增配置项 |
| 手动格挡移速 | 80 → 15 | 需新增配置项 |
| 手动格挡攻速 | 0.5s → 3s (10 → 60 ticks) | 需新增配置项 |
| 正面判定角度 | 120° | 需新增配置项 |

### 2.4 兵种持有规则

| 兵种 | 能否持有 | 手动格挡 | 被动格挡 | 出生状态 |
|------|---------|---------|---------|---------|
| 步兵 | ✅ | ✅ 移速→15, 攻速→3s, 正面 120° 全额挡伤 | ✅ 40% | 自带满 HP 盾牌 |
| 民兵 | ✅ | ❌ | ✅ 40% | 无盾 |
| 骑兵 | ✅ | ❌ | ✅ 40% | 无盾 |
| 弓兵 | ❌ | ❌ | ❌ | 无盾 |

### 2.5 系统流程

#### 2.5.1 出生

步兵从城池出生时，自动获得 `ShieldItem { hp: 1500, max_hp: 1500 }` 和 `ShieldState::Normal`。

#### 2.5.2 被动格挡（所有持盾士兵）

每次受到伤害时：
1. 独立判定 40% 概率
2. 命中 → 伤害转移到盾牌 HP（盾牌 HP 扣减，士兵 HP 不扣）
3. 未命中 → 正常扣士兵 HP

#### 2.5.3 手动格挡（仅步兵）

玩家发送 `SetShield` 命令切换 `ShieldState::Normal` ↔ `ShieldState::Blocking`。

**Blocking 状态下：**
- 移速: 80 → 15
- 攻速: 10 ticks → 60 ticks
- 正面 120° 扇形内的所有伤害: 100% 由盾牌承担
- 非正面伤害: 正常扣士兵 HP（不经过被动格挡判定）

**角度判定：**
- 以士兵当前面朝方向为中心，左右各 60° 的扇形（共 120°）
- 攻击来源在扇形内 → 全额由盾牌承担
- 攻击来源在扇形外 → 正常伤害

#### 2.5.4 盾牌破碎

当盾牌 HP ≤ 0 时：
- 移除士兵的 `ShieldItem` 和 `ShieldState` 组件
- 盾牌消失，不掉落

#### 2.5.5 死亡掉落

当持有盾牌的士兵死亡时：
1. 在士兵位置生成 `DroppedShield` 实体
2. `DroppedShield` 包含原盾牌的 HP 数据
3. 记录 `drop_tick` 用于计时

#### 2.5.6 掉落盾牌生命周期

```
tick 0        : 盾牌掉落，开始存在于地面
tick 600 (30s): 开始 3 秒消失动画
tick 660 (33s): 动画结束，盾牌实体销毁
```

#### 2.5.7 拾取检测

每 tick，检测所有士兵附近的 `DroppedShield`（检测距离 = 碰撞半径 + 盾牌拾取半径）：

```
if 士兵是弓兵:
    跳过
if 士兵无盾:
    直接拾取（移除 DroppedShield，给士兵添加 ShieldItem）
if 士兵有盾:
    if 地上盾牌 HP > 士兵当前盾牌 HP:
        拾取新盾牌，丢弃旧盾牌（旧盾牌不掉落，直接消失）
    else:
        跳过
```

#### 2.5.8 城池光环回复

盾牌 HP 与士兵 HP 一样，在城池光环范围内受到相同的回复效果。
- 回复量: `base_heal + city_level * per_level_heal`（与士兵 HP 回复相同）
- 回复上限: 不超过 `max_hp`

### 2.6 content/combat.ron 新增配置

```ron
shield: (
    // 现有配置保留
    speed_penalty: 15,           // 修改：80→15
    damage_reduction: 1.0,       // 修改：手动格挡时正面全额挡伤
    intercept_chance: 0.70,      // 保留（或移除？见下方问题）

    // 新增配置
    initial_hp: 1500,            // 盾牌初始 HP
    passive_block_chance: 0.40,  // 被动格挡概率
    frontal_angle: 120,          // 正面判定角度（度）
    attack_speed_penalty: 60,    // 手动格挡时攻击间隔（ticks）
    drop_survive_ticks: 600,     // 掉落存活时间（ticks）
    disappear_animation_ticks: 60, // 消失动画时长（ticks）
),
```

---

## 3. 弓兵行为修正

### 3.1 Multi-shot（新增）

**触发条件：**
- 弓兵攻击冷却结束时，按概率判定是否触发 multi-shot
- 概率公式: `base_chance + level * per_level_bonus`，上限 `max_chance`
- 当前配置: `base_chance: 0.05, per_level_bonus: 0.005, max_chance: 0.30`

**触发行为：**
- 从射程内随机选择 2-5 个不同敌人（每个敌人最多被选中一次）
- 对每个目标各射一箭（独立的 Arrow 实体）
- 所有箭矢共享同一个发射位置（弓兵位置）

**冷却：**
- 触发 multi-shot 后，正常进入 0.5s 攻击冷却

**目标选择：**
- 从射程内的所有敌人士兵中随机选择
- 如果射程内敌人数不足 multi-shot 数量，则射出对应数量的箭（比如只有 2 个敌人就射 2 箭）
- 如果射程内没有敌人，不触发 multi-shot

### 3.2 追击行为（修改）

**当前行为：**
- 弓兵在 `Fighting` 状态下完全不移动

**修改后行为：**
- 弓兵在 `Fighting` 状态下，如果当前目标离开射程：
  1. 弓兵向目标方向移动，追击到射程边缘
  2. 移动过程中如果目标重新进入射程，继续射击
  3. 移动过程中不射击（边走边射暂不实现，保持简单）

**实现方式：**
- 修改 `soldier_movement_system` 中的弓兵特殊逻辑
- 移除"弓兵在 Fighting 状态下跳过移动"的硬编码
- 改为：弓兵在 Fighting 状态下，检查目标是否在射程内
  - 在射程内 → 不移动，继续射击
  - 不在射程内 → 向目标移动，不射击

### 3.3 移除近距离伤害惩罚

**移除内容：**
- `combat.ron` 中的 `archer_melee_range: 50` 和 `archer_melee_damage_mult: 0.85`
- 代码中相关的近距离判定逻辑

---

## 4. 设计文档同步更新

需要更新 `docs/superpowers/specs/2026-06-05-rts-game-design.md`：

| 更新项 | 原文 | 修改为 |
|--------|------|--------|
| 箭矢飞行方式 | "400px/s 追踪速度" | "固定方向飞行，速度 400 单位/秒" |
| 弓兵射程 | "600px" | "380px (Lv.1) → 600px (Lv.4)，线性缩放" |
| 近距离惩罚 | "≤50px 伤害减少 15%" | 移除此描述 |
| 步兵弓箭减伤 | "受到弓兵伤害减少 10%" | 移除此描述 |
| 盾牌模式 | "移速降30，80%正面减伤，70%拦截" | 全新盾牌物品系统（见本文档） |
| 骑兵闪避 + 盾牌 | 无 | 新增：闪避优先判定，闪避未命中后判定被动格挡 |

---

## 5. 实现优先级建议

1. **P0 — 盾牌系统核心**: 数据结构、被动格挡、破碎、掉落、拾取
2. **P0 — 弓兵 Multi-shot**: 补齐 multi-shot 逻辑
3. **P1 — 弓兵追击行为**: 修改 Fighting 状态下的移动逻辑
4. **P1 — 清理**: 移除近距离惩罚、移除旧盾牌配置
5. **P2 — 视觉**: 盾牌白色血条、掉落动画、消失动画
6. **P2 — 设计文档更新**: 同步所有变更

---

## 6. 待确认事项

以下事项在设计讨论中未明确，需要确认：

1. **`combat.ron` 中的 `intercept_chance: 0.70`（箭矢拦截概率）**：原有设计是步兵开盾时有 70% 概率拦截飞过的箭矢。新设计中，被动格挡已经统一为 40%。是否还需要保留"拦截飞过箭矢"的独立机制？还是统一用被动格挡替代？

2. **盾牌拾取半径**: 士兵检测附近掉落盾牌的距离是多少？等于碰撞半径？还是一个固定值？

3. **手动格挡的正面判定是否考虑攻击来源方向**: 当前设计是基于士兵面朝方向判定 120°。但士兵的"面朝方向"由什么决定？是移动方向？还是攻击目标方向？

4. **弓兵边走边射**: 当前设计明确"追击时不射击"。未来是否需要实现边走边射？
